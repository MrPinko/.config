plugin {
    hyprbars {
        bar_height = 25
        bar_text_size = 10
        # config
        # exludes non floating windows and windows that have one of these titles Easy Effects|Volume Control|Bluetooth|iwgtk
        exclude_windowrulev2 = floating:0
        buttons {
            # button config
        }
    }
}